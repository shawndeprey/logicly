class DefaultController < ApplicationController
  # skip_before_filter :require_session, :only => [:index]
  # skip_before_filter :require_verification, :only => [:index]
  # skip_before_filter :require_activation, :only => [:index]

  # GET /new/
  def index
  end
end